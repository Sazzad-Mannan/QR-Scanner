<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QRScanner" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->

        <item name="titleTextColor">@color/colorAccent</item>
        <item name="subtitleTextColor">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#F2EAEA</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="BlackButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@android:color/black</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="backgroundTint">@null</item>
    </style>

    <style name="Theme.QRScanner" parent="Base.Theme.QRScanner" />
</resources>